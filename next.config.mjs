/** @type {import('next').NextConfig} */
const nextConfig = {
  // typedRoutes can be re-enabled once the app stabilises; it's strict with
  // dynamic href strings and creates friction for a starter.
};

export default nextConfig;
